{
  "name": "carbonsyncd",
  "version": "2.0.0-alpha.2",
  "description": "CarbonSync 2.0 — headless LAN file-sync daemon, managed via Carbon Cortex MCP",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@parcel/watcher": "^2.5.6",
    "better-sqlite3": "^12.8.0",
    "ignore": "^7.0.5",
    "pino": "^10.3.1",
    "selfsigned": "^2.4.1"
  }
}
